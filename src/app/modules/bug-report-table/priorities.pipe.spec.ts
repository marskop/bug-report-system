import { PrioritiesPipe } from './priorities.pipe';

describe('PrioritiesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrioritiesPipe();
    expect(pipe).toBeTruthy();
  });
});
